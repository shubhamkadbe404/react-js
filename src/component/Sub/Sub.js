const Sub = (props) => {
    return(
        <div>
            <h1>This is the Substraction Function</h1>
            <p>Substraction of 2203 & 352 number's = {parseInt(props.a) - parseInt(props.b)}</p>
        </div>
    )
};

export default Sub;